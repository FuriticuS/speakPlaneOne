// Геометрия и ёмкость блоков (мировые координаты, MVP-оценки).
// Точные значения подстроим под шрифт фронта позже.

export const BLOCK = {
  // Диапазон случайных размеров нового блока (мировые единицы).
  MIN_SIZE: 80,
  MAX_SIZE: 240,

  // Оценка размера одного символа: ширина × высота строки.
  // Ёмкость = floor(площадь / (CHAR_WIDTH * LINE_HEIGHT)).
  CHAR_WIDTH: 8,
  LINE_HEIGHT: 14,

  // Радиус поиска соседних блоков при клике по пустому месту.
  SEARCH_RADIUS: 400,
};

export const BLOCK_EDGES = ['north', 'south', 'east', 'west'];
