# Changelog

Все значимые изменения проекта будут описаны в этом файле.

## [v1.0.0] - 2026-09-01

### Fixed
- `POST /blocks`: неиспользуемый параметр `$2` в SQL поиска соседних блоков (Postgres: «could not determine data type of parameter $2») — SQL и параметры приведены в соответствие (`$1`=радиус, `$2`=x, `$3`=y)

### Changed
- Алгоритм роста блоков («магнит»):
  - грань, обращённая к родителю (`OPPOSITE_EDGE`), теперь считается занятой — блок не может лечь поверх своего родителя
  - проверка пересечений (`rectsOverlap`) со всеми существующими блоками — новые блоки не перекрывают уже размещённые (в т.ч. на углах между ветками)
  - примагничивание к ближайшему к точке клика блоку (`distanceToRect` + сортировка), а не к случайному в радиусе
- `createBlock`: перебор кандидатов → граней → случайных размеров до первого свободного места; если свободного места рядом нет — ошибка `No free space nearby` (400)

### Notes
- Рост полотна детерминирован геометрией: каждый блок занимает ровно одну грань родителя, свободные грани остаются для дальнейшего роста

## [v0.0.4] - 2026-09-01

### Added
- Поле `capacity` в ответах блоков: хелпер `withCapacity()` в `blocksService.js` — `listBlocks`, `getBlockById`, `createBlock`, `updateBlock` теперь возвращают ёмкость блока (сколько символов помещается)

### Notes
- Ёмкость считается из геометрии блока через `capacity(width, height)` — единый источник истины на бекенде
- Фронтенд использует `capacity` для лимита ввода в инлайн-редакторе

## [v0.0.3] - 2026-08-29

### Added
- Новая модель данных: `blocks` (блоки-«страны» с геометрией `x/y/width/height`, `parent_id`, `edge`, `owner_id`) вместо старой `Project → Page → Block`
- Профильные поля `users`: `name`, `gender`, `age` (заполняются на `/settings` после регистрации)
- `config/constants.js`: константы геометрии (размеры блоков, размер символа/строки для ёмкости, радиус поиска соседей)
- `modules/blocks/blocksGeometry.js`: чистая геометрия — `capacity()`, `positionForEdge()`, `parseBbox()`
- Алгоритм роста «магнит»: клик по пустому → блоки рядом → у кого свободная грань → случайный выбор → приклеить блок случайного размера
- Эндпоинты блоков: `GET /blocks?bbox=`, `GET /blocks/:id`, `POST /blocks`, `PUT /blocks/:id` (запись один раз, проверка ёмкости), `DELETE /blocks/:id` (только admin)
- Модуль профиля: `GET/PUT /profile`
- Сид корневого блока в `(0,0)` с текстом «Добро пожаловать в SpeakPlane»
- Роль `admin`: удаление любых блоков (модерация); обычный user — создать + записать свой блок один раз

### Changed
- `init-db.js`: новые таблицы + профильные поля + удаление старых `projects`/`pages`/`blocks`
- `server.js`: маршруты `/blocks` и `/profile`, убраны старые `/projects` и `/pages`
- Схемы Zod: новые `blockCreateSchema`/`blockUpdateSchema`/`blockParamsSchema`/`blockBboxQuerySchema`, `profileSchema`

### Removed
- Старые модули `modules/projects` и `modules/pages`
- Старые схемы `page`/`project`/`post`/`profile`

### Notes
- Все 29 тестов проходят (`node --test`)
- Права: guest — смотреть, user — создавать + писать один раз, admin — удалять
- Редактирование текста после первой записи запрещено (409)

## [v0.0.2] - 2026-08-11

### Added
- Полный CRUD для pages (5 эндпоинтов): список, создание, получение, обновление, удаление
- Полный CRUD для blocks (5 эндпоинтов): список, создание, получение, обновление, удаление
- Права доступа на всех уровнях (guest / user / admin):
    - guest — только публичные проекты
    - user — свои проекты + публичные
    - admin — всё
- Проверка принадлежности страницы проекту в blocks-сервисе (защита от доступа к чужим данным)
- 55 unit-тестов: `tests/pages.test.js`, `tests/blocks.test.js`, `tests/errors.test.js`
- Тесты на схемы валидации Zod для pages и blocks
- Тесты на логику прав доступа (guest/user/admin)
- Тесты на AppError и коды ошибок (401, 403, 404, 409)
- CORS origin вынесен в `CORS_ORIGIN` (переменная окружения)

### Changed
- `blocksService.js` — все 5 методов теперь проверяют, что `page.project_id === projectId`
- `server.js` — CORS использует `process.env.CORS_ORIGIN || true`
- Унифицирован формат JSON-ответов: все эндпоинты возвращают `{ success, data }`

### Notes
- Backend полностью готов к интеграции с фронтендом
- Auth: accessToken в теле, refreshToken в httpOnly cookie, ротация токенов
- Все 58 тестов проходят успешно

## [v0.0.1] - 2026-08-04

### Added
- Базовая структура Express API
- Аутентификация backend: регистрация, вход, выход, получение текущего пользователя
- JWT: access token и refresh token
- Работа с PostgreSQL через пул подключений
- Валидация входных данных с помощью Zod
- Схемы для основных сущностей: project, page, block
- Базовые модули по структуре: routes, controllers, services
- Подключение новых маршрутов к серверу
- Таблицы в базе данных для пользователей, refresh token, проектов, страниц и блоков
- Базовые тесты для проверки ключевой логики и валидации
- Создали базу postgresql пароль как обычно порт 5432

### Changed
- Организована модульная структура backend-проектa для дальнейшего расширения
- Вынесена логика валидации и схемы в отдельные модули

### Notes
- Это первая рабочая версия чернового backend-этапа
- Проект готов для дальнейшего развития и добавления полноценной бизнес-логики
